const ApiConfig = (function () {
    const base_url = 'https://api.products.com';
    const version = 'v1';
    const url = base_url + '/api/' + version;

    function getConfig() {
        return {
            getBaseUrl: () => base_url,
            getUrl: () => url
        };
    }

    return { getConfig };
})();
